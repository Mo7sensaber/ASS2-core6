using ASS1.Contexts;
using ASS1.Entities;
using ASS1.Migrations;

namespace ASS1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region 1.Do CRUD operations on all tables (Use Previous Assignment)

            //student student = new student()
            //{
            //    FName = "mohsen",
            //    LName = "saber",
            //    address = "giza",
            //    Age = 21,
            //    Dep_Id = 1
            //};
            //Contexet cont = new Contexet();
            //cont.Add(student);
            //Console.WriteLine(cont.Entry(student).State);
            ////cont.SaveChanges();
            //Console.WriteLine(cont.Entry(student).State);

            //student.FName = "Ahmed";
            //Console.WriteLine(cont.Entry(student).State);

            ////cont.SaveChanges();

            //var st = cont.students.FirstOrDefault(e => e.Id == 3);
            //Console.WriteLine(cont.Entry(st).State);

            //cont.Remove(st);
            //Console.WriteLine(cont.Entry(st).State);
            //cont.SaveChanges();

            #endregion


        }
    }
}